# Fabric AI Environment Configuration Template
# Copy this file to .env and update with your actual values

# =============================================================================
# CORE FABRIC CONFIGURATION
# =============================================================================

# Default AI model to use (must be available in Ollama or Gemini)
FABRIC_DEFAULT_MODEL="llama3.1"

# Ollama API URL (use internal Docker service name for container communication)
FABRIC_OLLAMA_URL="http://ollama:11434"

# Output directory for generated files
FABRIC_OUTPUT_DIR="$HOME/fabric_output"

# Patterns directory (uses Fabric defaults if not specified)
FABRIC_PATTERNS_DIR="$HOME/.config/fabric/patterns"

# =============================================================================
# API SECURITY CONFIGURATION
# =============================================================================

# API key for REST API authentication (CHANGE THIS!)
# Generate a strong key: openssl rand -base64 32
FABRIC_API_KEY=" "

# Require authentication for API access
FABRIC_REQUIRE_AUTH=true

# =============================================================================
# CLOUD AI SERVICES (OPTIONAL)
# =============================================================================

# Google Gemini API configuration
# Get your API key from: https://makersuite.google.com/app/apikey
FABRIC_GEMINI_API_KEY=""
FABRIC_GEMINI_API_URL="https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent"

# OpenAI API configuration (if needed)
# FABRIC_OPENAI_API_KEY="sk-your-openai-api-key-here"
# FABRIC_OPENAI_API_URL="https://api.openai.com/v1"

# Anthropic Claude API configuration (if needed)
# FABRIC_ANTHROPIC_API_KEY="your-anthropic-api-key-here"
# FABRIC_ANTHROPIC_API_URL="https://api.anthropic.com/v1"

# =============================================================================
# TRAEFIK REVERSE PROXY CONFIGURATION
# =============================================================================

# Your domain name for SSL certificates and routing
TRAEFIK_DOMAIN="rohi.life"

# Subdomain for Fabric REST API (e.g., fabric-ai.rohi.life)
FABRIC_AI_SUBDOMAIN="fabric-ai"

# Subdomain for Fabric Web Interface (e.g., fabric.rohi.life)
FABRIC_WEB_SUBDOMAIN="fabric"

# =============================================================================
# DOCKER COMPOSE CONFIGURATION
# =============================================================================

# Enable Docker Compose Bake for better build performance
COMPOSE_BAKE=true

# =============================================================================
# DEVELOPMENT SETTINGS (OPTIONAL)
# =============================================================================

# Set to development for verbose logging
# NODE_ENV="development"

# Fabric debug mode
# FABRIC_DEBUG=true

# =============================================================================
# SECURITY RECOMMENDATIONS
# =============================================================================

# 1. Generate a unique API key:
#    openssl rand -base64 32
#
# 2. Use environment-specific .env files:
#    .env.development, .env.production, .env.staging
#
# 3. Never commit .env files to version control
#
# 4. Restrict API access with Traefik IP allowlists:
#    Add middlewares for IP filtering in Traefik configuration
#
# 5. Use strong SSL certificates:
#    Configure Let's Encrypt or custom certificates in Traefik
#
# 6. Monitor API usage:
#    Check Docker logs regularly: docker logs fabric-ai
#
# 7. Backup configuration:
#    Keep backup copies of working .env configurations

# =============================================================================
# EXAMPLE USAGE
# =============================================================================

# After setting up this file:
# 1. Copy to .env: cp .env.example .env
# 2. Edit values: nano .env
# 3. Start services: docker-compose up -d
# 4. Test API: curl -H "X-API-Key: YOUR_KEY" http://localhost:8999/models/names
# 5. Access web: http://localhost:5173 or https://fabric.yourdomain.com
